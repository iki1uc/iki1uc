name: iki1uc-Sensorik

on:
  workflow_run:
    workflows: ["OP-Kern"]
    types: [completed]

jobs:
  sensorik:
    runs-on: ubuntu-latest
    steps:
      - name: Sensorik liefert Rohdaten an OP
        run: echo "iki1uc → OP: Sensorik-Daten übertragen"
